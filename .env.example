DB_USER=
DB_PASSWORD=replace_with_database_password
DB_NAME=
# Use postgres-db when the Nest app runs in Docker; localhost when it runs on your host.
DB_HOST=
DB_PORT=
PGADMIN_EMAIL=
PGADMIN_PASSWORD=replace_with_pgadmin_password
PORT=
